syntax= "proto3";

package books_storage;
option go_package = "./books_storage";

message Authors {
    repeated string AuthorName = 1;
}

message Books {
    repeated string BookName = 1;
}

service BooksStorage {
    rpc GetBooksByAuthors(Authors) returns (Books){}
    rpc GetAuthorsByBooks(Books) returns (Authors){}
}